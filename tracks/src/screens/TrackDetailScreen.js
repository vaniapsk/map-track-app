import React from "react";
import { StyleSheet, Text, View } from "react-native";

const TrackDetailScreen = () => {
    return <View>
        <Text style={style.text}>Track Detail Screen</Text>
    </View>;
};

const style = StyleSheet.create({
    text: {
        fontSize: 25
    }
});

export default TrackDetailScreen;