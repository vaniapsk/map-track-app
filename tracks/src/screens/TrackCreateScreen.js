import React from "react";
import { StyleSheet, Text, View } from "react-native";

const TrackCreateScreen = () => {
    return <View>
        <Text style={style.text}>Track Create Screen</Text>
    </View>;
};

const style = StyleSheet.create({
    text: {
        fontSize: 25
    }
});

export default TrackCreateScreen;